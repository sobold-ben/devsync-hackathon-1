// 🎯 Frontend UI to be built by the devs:
// - Message list (fetched from API)
// - Input form (name + message)
// - Submit via POST
// - Poll or refresh to update

export default function Home() {
    return (
        <main>
            <h1>Microtweet Wall</h1>
            {/* TODO: Message feed area */}

            {/* TODO: Input form at bottom */}
        </main>
    );
}
